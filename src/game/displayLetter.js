import React from 'react';
import Letter from './letter'

export default function DisplayLetters(props){

    // let lettersArr = this.props.letters.map((letter, index) => {
    //     <Letter key={index} value={letter} />
    // });
    return(
        <div>
            <ul>
                {/* {lettersArr} */}
            </ul>
        </div>
    )
}